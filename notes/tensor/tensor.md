=================================
PREREQUISITE

Reading
1) What is it
--------------
Lists
2) WHAT IS ITS PURPOSE?
-----------------------
identity
    enforces distinction between elements
    
    for example: two people aged 32 stay separate in a list `(32, 32)` instead of collapsing into `{32}` in a set.

structure
    preserves the contextual meaning of each position

    for example: in `(weight, age)`, index 0 always tracks weight, ensuring `(72, 32)` and `(46, 12)` remain comparable.

mapping
    translates abstract multi-dimensional coordinates to flat physical memory
    for example: index formulas project a 2D matrix coordinate like `(row, col)` directly onto a 1D sequence of hardware RAM addresses.

F^n
Matrix
Matrix multiplication
