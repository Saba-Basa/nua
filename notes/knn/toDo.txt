
1. norms_lp
   - ℓₚ norms (L1, L2, L∞)
   - relation between norm and distance

2. dimensions
   - doubling dimension
   - packing / covering numbers
   - curse of dimensionality

3. knn_rules
   - distance measures
   - neighbor selection N_k(x)
   - classification (voting, weighted)
   - regression (averaging)
   - bias–variance behavior

4. trees
   - kd-tree, ball tree, cover tree
   - pruning via triangle inequality

5. lsh
   - p-/s-stable hashing
   - sensitivity, collision probability
   - approximate nearest neighbor